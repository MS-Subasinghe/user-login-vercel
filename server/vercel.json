{
    "version": 2,
    "builds": [
      { "src": "*.js", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/" },
      {"src": "/server/(.*)",
      "dest": "/server/routes/user.js"}
    ]
  }
  
